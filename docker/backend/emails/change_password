## Email sent to DSpace users when they forget their password.
##
## Parameters: {0} is expanded to a special URL
##
## See org.dspace.core.Email for information on the format of this file.
##
#set($subject = "${config.get('dspace.name')}: Solicitação de alteração de senha")
#set($phone = ${config.get('mail.message.helpdesk.telephone')})
Para alterar a senha da sua conta no RIGEO ${config.get('dspace.name')}, clique no link abaixo:

  ${params[0]}

Se precisar de ajuda com sua conta, envie um e-mail ${config.get("mail.helpdesk")}
#if( $phone )
ou entre em contato pelo telefone ${phone}.
#end

Equipe RIGEO